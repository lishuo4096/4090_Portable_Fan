C51 COMPILER V9.60.7.0   UART1                                                             05/08/2026 19:19:29 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE UART1
OBJECT MODULE PLACED IN .\Objects\UART1.obj
COMPILER INVOKED BY: C:\Users\lishuo\AppData\Local\Keil_v5\C51\BIN\C51.EXE UART1.c LARGE OMF2 OPTIMIZE(7,SPEED) BROWSE I
                    -NCDIR(..\..\C51) DEBUG PRINT(.\Listings\UART1.lst) OBJECT(.\Objects\UART1.obj)

line level    source

   1          #include "STC8H.H"
   2          #include "main.h"
   3          /*
   4          本来打算做成环形缓冲区的
   5          但是环形环形缓冲区有点太费ram了
   6          而且在串口波特率设置为1M之后，串口阻断循环的时间也是可以接受的
   7          实际上即使是115200波特率，发7字节也只需要500us
   8          */
   9          //底层阻断式发送函数
  10          void Serial_SendByte(uint8_t dat) {
  11   1          SBUF = dat;           // 写入发送缓冲区
  12   1          while (!TI);          // 等待发送完成（TI=1）
  13   1          TI = 0;               // 清除发送中断标志
  14   1      }
  15          
  16          void Serial_Printstr(uint8_t *str){//打印字符串
  17   1          while (*str != '\0') {
  18   2              Serial_SendByte(*str);
  19   2              str++;
  20   2          }
  21   1      }
  22          
  23          void Serial_Printstrln(uint8_t *str){//打印字符串并换行
  24   1          while (*str != '\0') {
  25   2              Serial_SendByte(*str);
  26   2              str++;
  27   2          }
  28   1          Serial_SendByte('\r');
  29   1          Serial_SendByte('\n');
  30   1      }
  31          
  32          void Serial_Printnumln_with_Word(uint8_t *str,uint16_t num){//带语句和换行的格式化输出
  33   1          int8_t buffer[7];//声明缓冲区，只需要6位，多一位防止溢出
  34   1          int8_t *p = buffer + sizeof(buffer) - 1;//定义指针，使指针指向数组末尾,因为要从后往前写入字符
  35   1          //整数转字符
  36   1          if (num == 0) {
  37   2              *(--p) = '0';
  38   2          } else {
  39   2              while (num > 0) {
  40   3                  *(--p) = (num % 10) + '0';  // 转为 ASCII
  41   3                  num /= 10;
  42   3              }
  43   2          }
  44   1          Serial_Printstr(str);
  45   1          Serial_Printstr(p);
  46   1          Serial_SendByte('\r');
  47   1          Serial_SendByte('\n');
  48   1      }
  49          
  50          void Serial_Printnum(uint16_t num){//格式化输出，不换行
  51   1          int8_t buffer[7];//声明缓冲区，只需要6位，多一位防止溢出
  52   1          int8_t *p = buffer + sizeof(buffer) - 1;//定义指针，使指针指向数组末尾,因为要从后往前写入字符
  53   1          *p = '\0';       // 字符串结尾为\0
  54   1          //整数转字符
C51 COMPILER V9.60.7.0   UART1                                                             05/08/2026 19:19:29 PAGE 2   

  55   1          if (num == 0) {
  56   2              *(--p) = '0';
  57   2          } else {
  58   2              while (num > 0) {
  59   3                  *(--p) = (num % 10) + '0';  // 转为 ASCII
  60   3                  num /= 10;
  61   3              }
  62   2          }
  63   1          //发送转换的字符
  64   1          Serial_Printstr(p);
  65   1      }
  66          void Serial_Printint16ln(int16_t num){//格式化输出int16并换行
  67   1          int8_t buffer[7];//声明缓冲区，只需要6位，多一位防止溢出
  68   1          int8_t *p = buffer + sizeof(buffer) - 1;//使指针指向数组末尾,因为要从后往前写入字符
  69   1          if(num < 0){
  70   2              num = (uint16_t)(-1 * (int32_t)num); 
  71   2              Serial_SendByte('-');//先传负号
  72   2          }
  73   1          else{
  74   2              num = (uint16_t)num;
  75   2          }
  76   1          
  77   1          *p = '\0';       // 字符串结尾为\0
  78   1      
  79   1          if (num == 0) {
  80   2              *(--p) = '0';
  81   2          } else {
  82   2              while (num > 0) {
  83   3                  *(--p) = (num % 10) + '0';  // 转为 ASCII
  84   3                  num /= 10;
  85   3              }
  86   2          }
  87   1      
  88   1          // 先发送数字
  89   1          Serial_Printstr(p);
  90   1          //再发送换行（Windows 常用 \r\n，Linux/Mac 可只用 \n）
  91   1          Serial_SendByte('\r');
  92   1          Serial_SendByte('\n');
  93   1      }
  94          void Serial_Printnumln(uint16_t num){//格式化输出并换行
  95   1          int8_t buffer[7];//声明缓冲区，只需要6位，多一位防止溢出
  96   1          int8_t *p = buffer + sizeof(buffer) - 1;//使指针指向数组末尾,因为要从后往前写入字符
  97   1          *p = '\0';       // 字符串结尾为\0
  98   1      
  99   1          if (num == 0) {
 100   2              *(--p) = '0';
 101   2          } else {
 102   2              while (num > 0) {
 103   3                  *(--p) = (num % 10) + '0';  // 转为 ASCII
 104   3                  num /= 10;
 105   3              }
 106   2          }
 107   1      
 108   1          // 先发送数字
 109   1          Serial_Printstr(p);
 110   1          //再发送换行（Windows 常用 \r\n，Linux/Mac 可只用 \n）
 111   1          Serial_SendByte('\r');
 112   1          Serial_SendByte('\n');
 113   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
C51 COMPILER V9.60.7.0   UART1                                                             05/08/2026 19:19:29 PAGE 3   

   CODE SIZE        =    751    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =   ----      51
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
   EDATA SIZE       =   ----    ----
   HDATA SIZE       =   ----    ----
   XDATA CONST SIZE =   ----    ----
   FAR CONST SIZE   =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
